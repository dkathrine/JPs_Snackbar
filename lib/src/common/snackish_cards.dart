import 'package:flutter/material.dart';

class SnackishCards extends StatelessWidget {
  const SnackishCards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
