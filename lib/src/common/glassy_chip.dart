import 'package:flutter/material.dart';

class GlassyChip extends StatelessWidget {
  const GlassyChip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
